# harmony

Peer-to-peer chat and voice in the browser. No accounts, no servers, no downloads — just a name and a link.

## Run locally

```bash
python3 -m http.server 8000
```

Then open [http://localhost:8000](http://localhost:8000).

## Run tests

```bash
npm install
npx playwright install
npx playwright test
```
